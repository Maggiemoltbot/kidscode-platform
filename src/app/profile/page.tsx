import { ProfileDashboard } from "@/components/profile/profile-dashboard";

export const metadata = {
  title: "Profil | KidsCode",
};

export default function ProfilePage() {
  return <ProfileDashboard />;
}
